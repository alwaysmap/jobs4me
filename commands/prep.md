---
description: Generate company overview and interview prep for a tracked role.
argument-hint: "<company name>"
---

Invoke the `prep` skill with the user's arguments: `$ARGUMENTS`.

If no company is provided, list roles in interviewing/applied stages so the user can pick one.
