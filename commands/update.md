---
description: Update a role's status — applied, interviewing, declined, rejected, offered.
argument-hint: "[Company - status [note]]"
---

Invoke the `update` skill with the user's arguments: `$ARGUMENTS`.

If no arguments were provided, the skill should list the user's active roles so they can pick one to update.
