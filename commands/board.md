---
description: Regenerate the kanban board from current tracker state.
---

Invoke the `board` skill to rebuild `Kanban/index.html` from `tracker.yaml`. The board normally auto-rebuilds on every mutation; use this to force a rebuild after editing YAML by hand.
