---
description: Draft a cover letter (and optionally a tailored resume) for a tracked role.
argument-hint: "<company name>"
---

Invoke the `apply` skill with the user's arguments: `$ARGUMENTS`.

If no company is provided, list active roles so the user can pick one.
