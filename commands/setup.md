---
description: Set up your job search profile, role types, and search sources.
---

Invoke the `setup` skill. On first run it walks through onboarding (full or quick); on subsequent runs it detects existing config and helps fill gaps.
