---
description: Adjust profile, role types, sources, target companies, or filters.
argument-hint: "[what to change, e.g. 'add Stripe to dream list']"
---

Invoke the `tweak` skill with the user's arguments: `$ARGUMENTS`.

If no arguments are provided, show the user the categories of things that can be adjusted.
