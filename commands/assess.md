---
description: Assess a job posting URL against your profile.
argument-hint: "<job posting URL>"
---

Invoke the `assess` skill with the user's arguments: `$ARGUMENTS`.

If no URL is provided, ask the user for one (or accept a pasted job description directly).
