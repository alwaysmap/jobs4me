---
description: Walk through tracked roles in batch — triage suggestions, advance, decline.
argument-hint: "[stage filter: suggested|maybe|applied|interviewing|stale|all]"
---

Invoke the `review` skill with the user's arguments: `$ARGUMENTS`.

If no argument is provided, review everything that needs attention in default priority order. Otherwise treat the argument as a stage filter.
